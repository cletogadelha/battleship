spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect
  datasource:
    url: jdbc:h2:mem:battleship;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    
grid:
  size: 10